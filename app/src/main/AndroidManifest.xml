<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mclohrk.appcampeonatosurf">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BateriaSurfistListActivity"></activity>
        <activity android:name=".ListarBateriaActivity" />
        <activity android:name=".BateriaActivity" />
        <activity android:name=".ListarSurfistaActivity" />
        <activity android:name=".Nota_Activity" />
        <activity android:name=".SurfistaActivity" />
        <activity android:name=".PrincipalActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>